id: repro/wire-formats
language: typescript
layer: library
tags:
- type.package
tasks:
  build:
    command: pnpm run build
    inputs:
    - glob: src/**/*.tdls
      cache: true
    outputs:
    - src/generated/**/*
toolchain:
  default: node
