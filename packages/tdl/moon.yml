id: repro/tdl
language: typescript
tags:
- type.package
tasks:
  build:
    command: pnpm run build
    deps:
    - ^:build
    inputs:
    - file: src/cli/grammar.ohm
    outputs:
    - src/cli/grammar.ohm-bundle.*
    options:
      cache: true
toolchain:
  default: node
